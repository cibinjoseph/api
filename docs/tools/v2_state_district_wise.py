tree = Node('(array)', [
    Node('(object)', [
        Node('"state" (string)'),
        Node('"districtData" (array)', [
            Node('"(object)"', [
                Node('"district" (string)'),
                Node('"confirmed" (integer)'),
                Node('"lastupdatedtime" (string)'),
                Node('"delta" (object)', [
                    Node('"confirmed" (integer)')
                ])
            ])
        ])
    ])
])
